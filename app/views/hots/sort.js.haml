//refresh image
$("ul#nav li a.dom_indexmenu").css('background-image', 'url(#{current_user.store.pages.homepage.menu_sprite_url}?#{Time.now.to_i})');
//resort
$("ul#nav li a.dom_indexmenu").parent().removeAttr('class').each(function(index){
$(this).addClass('nav_0' + (index + 1));
});
