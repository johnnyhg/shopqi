//hide
$.modal.close();

var parent = $("##{id(resource)}").parent();
$("##{id(resource)}").remove();

-unless resource.root?
  parent.children().removeClass('no_bar');
  parent.children(':first').addClass('no_bar');

//make ul:empty can find empty parent
if($.trim(parent.text()) == '') parent.text('');
Admin.refresh();
