//tab menu
$("#edit_store > .secondary-navigation li").removeClass('active');
$("#store_payment").addClass('active');

//insert
$("#edit_store > .content > .inner").html('#{escape_javascript( render(:partial => "#{store.collection_name}/#{action_name}"))}');
