#welcome
  -if current_member
    ==您好，
    =link_to current_member_name, member_path
    。 
    =link_to destroy_member_session_path do
      %span.orange.b 退出登录
  -else
    您好，欢迎光临凡客诚品。
    =link_to new_member_session_path, :rel => :nofollow do
      %span.orange.b 登录
    \|
    =link_to new_member_session_path, :rel => :nofollow do
      %span.orange.b 注册
