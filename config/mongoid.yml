defaults: &defaults
  host: localhost
  # slaves:
  #   - host: slave1.local
  #     port: 27018
  #   - host: slave2.local
  #     port: 27019

development:
  <<: *defaults
  database: shopqi_development

test:
  <<: *defaults
  database: shopqi_test

staging:
  <<: *defaults
  uri: <%= ENV['MONGODB_URI'] %>
  database: shopqi_staging

# set these environment variables on your prod server
production:
  <<: *defaults
  uri: <%= ENV['MONGODB_URI'] %>
  database: shopqi_production
